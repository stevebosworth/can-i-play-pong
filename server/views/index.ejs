<% include header %>
  <h1>Is Anyone Playing Ping Pong Right Now?</h1>

  <h2 class="status">
    <% if (status) { %>
      Yup
    <% } else { %>
      Nope
    <% } %>
  </h1>
  </h2>

  <p>
    Last update from the table: <%= formattedTime %>
  </p>
  <script>
  $(document).ready( function () {
    var $status = $('.status');
    var socket = io.connect('http://localhost:3000');
    socket.on('news', function (data) {
      console.log(data);
      socket.emit('my other event', { my: 'data' });
    });
  });
  </script>
<% include footer %>
